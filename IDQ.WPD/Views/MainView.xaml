<Window x:Class="IDQ.WPF.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IDQ.WPF.Views"
        xmlns:ctrls="clr-namespace:IDQ.WPF.Controls"
        xmlns:VM="clr-namespace:IDQ.WPF.ViewModels"
        mc:Ignorable="d"
        Title="IDQ - TG Software" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <VM:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ctrls:ctrlNavigationBar DataContext="{Binding Navigator}" Grid.Row="0" />

        <ContentControl Content="{Binding Navigator.CurrentViewModel}" Grid.Row="1" />

        <Button Content="Test" Command="{Binding comTest}" Grid.Row="2"/>
    </Grid>
</Window>
